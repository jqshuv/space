{
  "name": "{{kebab name}}",
  "version": "0.1.0",
  "description": "{{description}}",
  "author": "{{contact}}",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "{{repository}}"
  },
  "license": "{{license}}"
}
